services:
  api:
    build: .
    container_name: python_automation_api
    ports:
      - "8000:8000"
    volumes:
      - automation_data:/data
    restart: unless-stopped

volumes:
  automation_data:
